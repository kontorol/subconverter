{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ -o base/subconverter $(find CMakeFiles/subconverter.dir/src/ -name \"*.o\") -static -lpcre2-8 -levent -lyaml-cpp -L/usr/lib64 -lcurl -lmbedtls -lmbedcrypto -lmbedx509 -lz -lquickjs -llibcron -O3 -s",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

        
